package mx.unam.ciencias.modelado.proyecto1.factory;

/**
 * Interfaz marcadora para los objetos manejables por el sitio.
 * Esta interfaz es importante para todas las entidades que puedan ser fabricadas mediante
 * el patrón factory.
 * Esto se hizo para marcar todos los objetos concretos que puedan ser fabricados.
 */
public interface ObjetoCheemsMart{}